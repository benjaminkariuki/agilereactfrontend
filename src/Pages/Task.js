const Task = () => {
  return (
    <div
      className="w-full"
      style={{ overflowY: "auto", maxHeight: "calc(100vh - 64px)" }}
    >
      <h1>Task Manager</h1>
    </div>
  );
};

export default Task;
